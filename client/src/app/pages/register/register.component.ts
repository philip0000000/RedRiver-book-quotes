import { Component } from '@angular/core';

@Component({
  selector: 'app-register',
  standalone: true,
  template: `
    <h2>Register Page</h2>
    <!-- Placeholder content -->
  `
})
export class RegisterComponent {}
