import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  standalone: true,
  template: `
    <h2>Login Page</h2>
    <!-- Simple placeholder until UI is built -->
  `
})
export class LoginComponent {}
