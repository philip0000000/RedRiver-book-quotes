import { Component } from '@angular/core';

@Component({
  selector: 'app-book-list',
  standalone: true,
  template: `
    <h2>Book List</h2>
    <!-- Shows all books later -->
  `
})
export class BookListComponent {}
